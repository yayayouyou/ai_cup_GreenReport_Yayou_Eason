name: esg_clean

steps:
  - normalize_esg_type:
      sort_labels: true
  - fill_empty_esg_type:
      fill_value: "Unknown"
  # normalize_timeline intentionally omitted:
  # training data uses more_than_5_years, predictions must match

name: clean_text
steps:
  - normalize_esg_type:
      sort_labels: true
  - fill_empty_esg_type:
      fill_value: "Unknown"
  - normalize_timeline:
      rename: {}
  - clean_text:           # 移除 markdown 符號、壓縮多餘空白與換行
      field: data

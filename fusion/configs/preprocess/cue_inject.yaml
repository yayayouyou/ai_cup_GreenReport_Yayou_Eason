name: cue_inject
steps:
  - normalize_esg_type:
      sort_labels: true
  - fill_empty_esg_type:
      fill_value: "Unknown"
  - normalize_timeline:
      rename: {}
  - normalize_fullwidth:
      field: data
  - inject_cues:
      field: data
